# Candidate image-input profile. GPU validation is pending.
# Add these values to .env; keep MODEL_DIR and the usual 256K settings.
ENABLE_VISION=1
VLLM_WNA16_STATIC_HOT_CACHE_SIZE=80
VISION_MAX_IMAGES=1
VISION_MAX_PIXELS=1048576
